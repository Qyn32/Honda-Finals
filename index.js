// Initialize the carousel
const myCarousel = document.getElementById('myCarousel');
const carousel = new bootstrap.Carousel(myCarousel);

// Set the interval for automatic sliding (3 seconds)
carousel.interval = 3000;